import * as constants from './document.constants'
import * as service from './document.service'
import { api } from './document.api'

export const document = {
  constants,
  service,
  api
}

export * as DocumentProps from './document.types'
